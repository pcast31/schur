- Trouve toutes les partitions pour WeakSchur(4) (=66).
- Pour build et run en mode release : cargo run --release
- Pour build en mode release : cargo build --release
- l'executable est dans ./target/release/
